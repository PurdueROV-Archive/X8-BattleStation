/*
initialization
	reads config file, if there is a config file
	else, loads defaults
connects to device
initialize callbacks
define functions for actions

keyboardInput:

*/
#ifndef KEYBOARDINPUT_H
#define KEYBOARDINPUT_H

#endif
